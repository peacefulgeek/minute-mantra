{
  "name": "minute-mantra",
  "version": "1.0.0",
  "description": "Minute Mantra PWA — One mantra. One minute. Every morning.",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "cd client && pnpm install && pnpm build",
    "start": "NODE_ENV=production node server/index.js",
    "dev": "concurrently \"cd server && node index.js\" \"cd client && pnpm dev\"",
    "seed": "node seeds/seed.js",
    "generate-vapid": "node scripts/generate-vapid.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
