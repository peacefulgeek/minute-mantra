// Prisma schema — for reference and migration tooling
// The app uses raw mysql2 queries for performance

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}
